const gridSection = document.getElementsByClassName('grid-section');
const contato = document.getElementsByClassName('contato');

console.log(gridSection); // Retorna uma HTMLCollection com os elementos de classe "grid-section"
console.log(gridSection[0]); // Retorna o primeiro elemento da lista